# Рабочий лог — заведение проекта NetworkedPhysics (Sandbox)

Дата: 2026-06-17. Ведётся, чтобы не начинать сначала в следующей сессии.

---

## Что за проект и где что лежит

- **Проект:** `Z:\NetworkedPhysics` — клон github.com/cem-akkaya/NetworkedPhysics.
- **Имя проекта внутри — `Sandbox`** (не "NetworkedPhysics"!): `Sandbox.uproject`, модуль/таргеты `Sandbox` / `SandboxEditor`.
- **Физические объекты** (ради чего всё): `Source/Sandbox/` → `Ball`, `MyPhysicsPawn`, `LoaderPawn` + `LoaderSimComponent`, `VehicleSim*`-компоненты.
- **Движок:** source-сборка `Z:\UnrealEngineSource_5_7` (UE **5.7.4**), собрана 15.06.
  - Зарегистрирована в реестре `HKCU\SOFTWARE\Epic Games\Unreal Engine\Builds` под GUID
    **`{404B5AD4-45C0-680F-5FD0-1FA2988CF222}`** → это значение `EngineAssociation` в `Sandbox.uproject`.
- В `Z:\UNREAL_ENGINE\` лежат ~6 старых мусорных попыток (NetworkedPhysicsProj*, NetCoreProj и т.п.) — **игнорировать**, канонический проект только `Z:\NetworkedPhysics`.

## Что сделали сегодня (по шагам)

1. **Определили проект** — нужен именно `Z:\NetworkedPhysics` (а не варианты в `Z:\UNREAL_ENGINE`).
2. **Удалили лишний клон**, который ошибочно создали в `Z:\UNREAL_ENGINE\NetworkedPhysics_clean`.
3. **Привязка к движку уже была правильной** (GUID выше) — менять не пришлось.
4. **Починили несовместимость с 5.7** (репозиторий писался под UE 5.8):
   - Файл `Source/SandboxEditor.Target.cs`: было
     `EngineIncludeOrderVersion.Unreal5_8` и `BuildSettingsVersion.V7`
     — этих значений в 5.7 НЕТ (максимум `Unreal5_7` / `V6`).
     Поменяли оба на **`.Latest`**.
   - (`Source/Sandbox.Target.cs` уже был на `V6` — не трогали.)
5. **Отключили 2 маркетплейс-плагина**, которых нет на диске и которые не нужны коду
   (`Sandbox.Build.cs` от них не зависит) — в `Sandbox.uproject` поставили `"Enabled": false`:
   - `ElectronicNodes` (косметика редактора)
   - `LowEntryExtStdLib` (BP-библиотека)
   Остальные плагины — движковые (Chaos*, Mover*, Iris и т.д.), есть в 5.7.
6. **Собрали** редакторский таргет:
   ```
   Z:\UnrealEngineSource_5_7\Engine\Build\BatchFiles\Build.bat SandboxEditor Win64 Development -Project="Z:\NetworkedPhysics\Sandbox.uproject" -WaitMutex
   ```
   Результат: **Succeeded** за ~50 сек, собран `Binaries\Win64\UnrealEditor-Sandbox.dll`.
   Лог сборки: `Z:\NetworkedPhysics\build_sandbox.log`.

## Проблема при первом запуске и её решение

- Первый запуск редактора **завис на сплеше**: процесс жив, но CPU/лог не растут.
- Причина по логу: **дедлок компиляции шейдеров через XGE/distributed-контроллер**.
  В логе: `Using XGE Controller for shader compilation`, воркеры запустились
  (`launch 14 ShaderCompileWorkers`), затем умерли, а редактор бесконечно ждал результатов
  (`======= ShaderCompileWorker-Distributed Diagnostics =======` каждые 15 минут, >1 часа).
- **Решение:** убить процесс и перезапускать редактор с флагом **`-noxgeshadercompile`**:
  ```
  Z:\UnrealEngineSource_5_7\Engine\Binaries\Win64\UnrealEditor.exe "Z:\NetworkedPhysics\Sandbox.uproject" -noxgeshadercompile
  ```
  После этого 16 воркеров реально заработали, и шейдеры **докомпилировались**
  (`LogShaderCompilers: Shaders left to compile 0`).

> ВАЖНО на будущее: всегда запускать редактор с `-noxgeshadercompile`,
> пока не разберёмся с XGEController (как вариант — отключить плагин `XGEController`
> или в `Config/DefaultEngine.ini` задать `[DevOptions.Shaders] bAllowCompilingThroughWorkers=False`).

## Текущее состояние (на момент остановки)

- Шейдеры скомпилированы (Shaders left to compile **0**).
- НО редактор **завершился** сразу после компиляции (в логе пошли строки shutdown:
  `Abandoning remaining async ... for shutdown`, `Destroying Remaining Audio Device(s)`).
  **Причину завершения (краш или штатный выход) ещё НЕ выяснили** — прервались на чтении конца лога.

## Следующие шаги (с чего продолжить)

1. Посмотреть конец `Z:\NetworkedPhysics\Saved\Logs\Sandbox.log` (или архив `Sandbox-hang-*.log`)
   на предмет `Fatal error` / `Assertion failed` / `=== Critical error ===` —
   понять, почему редактор закрылся после компиляции шейдеров.
2. Если краш — разобрать стек; если штатный выход — просто перезапустить:
   `UnrealEditor.exe "Z:\NetworkedPhysics\Sandbox.uproject" -noxgeshadercompile`
   (шейдеры уже в кэше, второй запуск должен быть быстрым).
3. После открытия редактора — открыть нужную карту из `Content/Maps` и проверить физические пешки.

---

## Новый класс: AModularCarPawn (модульный автомобиль, Lego-Fortnite стиль)

> ⚠️ УСТАРЕЛО (см. секцию 2026-06-18 ниже): архитектуру ClusterUnion ниже **заменили** на
> одиночное твёрдое тело `Body` (Cube) + raycast-подвеска. Текст оставлен для истории.

Файлы: `Source/Sandbox/ModularCarPawn.h` / `.cpp`. Собрано успешно (build_modularcar.log → Result: Succeeded).

**Архитектура (ИСТОРИЯ, заменена):** Cluster Union (как Lego Fortnite).
- Корень пешки — `UClusterUnionComponent` (симулируемое тело, в которое всё сваривается).
- Кузов — примитив `Cube` (`/Engine/BasicShapes/Cube`), коллидирует.
- Колёса — примитивы `Cylinder`, **PhysicsOnly + игнор всех коллизий** (тело-частица для кластера есть, об землю не бьются), машину держит **raycast-подвеска** (реально прикладывает силу).
- Сила к кластеру — через `FPhysicsObjectExternalInterface::LockWrite` → `AddForce`/`AddTorque` (у кластера нет BodyInstance). Сила в точке раскладывается на силу+момент вокруг `WorldCoM`.

**Рантайм add/remove колёс (как и просили):**
- `AddWheel(RelLoc, bDriven, bSteering, Radius, Width)` → создаёт цилиндр и `ClusterUnion->AddComponentToCluster(...)`.
- `RemoveWheel(Index)` / `RemoveLastWheel()` → `ClusterUnion->RemoveComponentFromCluster(...)` + destroy.
- Все три — BlueprintCallable.

**Езда (Конструкция + езда):** в `Tick → UpdateVehiclePhysics`:
1. raycast вниз от каждого колеса (вне физ-локов);
2. под write-локом: подвеска (пружина+демпфер, push-only), тяга (`EngineForcePerWheel`), руль (поворот направления тяги у `bSteering`-колёс), боковой захват, продольное торможение/откат.
- Ввод: `SetThrottleInput()`, `SetSteerInput()`, `SetHandbrake()` (BlueprintCallable) + legacy-оси `MoveForward`/`Turn`.
- На `BeginPlay` при `bSpawnDefaultWheels=true` ставит 4 колеса по углам (перед — рулевой).

**Тюнинг** (поля Details, категории Modular Car|*): `BodyExtent`, `BodyMassKg`, `SuspensionRestLength/Stiffness/Damping`, `EngineForcePerWheel`, `MaxSteerAngle`, `LateralGrip` (0..1), `LongitudinalGrip` (0..1).

**Что осталось проверить в редакторе:**
- Создать BP на основе `ModularCarPawn` (или заспавнить класс), поставить на карту, Possess, проверить езду.
- Подёргать `AddWheel`/`RemoveWheel` в рантайме (напр. с кнопки) — убедиться, что кластер перестраивается.
- Возможный риск: при слиянии геометрии кластер может всё же коллайдить колёсами об землю несмотря на игнор-ответы — если машина «подпрыгивает», переключить колёса на обычную коллизию (rigid-support) и убрать пружину, либо тюнить подвеску.
- Build не зависит от запущенного редактора; запускать редактор по-прежнему с `-noxgeshadercompile`.

---

## Гейммод: ModularCarPawn назначен пешкой по умолчанию (2026-06-17)

- Создан C++ гейммод `Source/Sandbox/ModularCarGameMode.h/.cpp` (`AGameModeBase`),
  `DefaultPawnClass = AModularCarPawn`, `PlayerControllerClass = APlayerController`.
- В `Config/DefaultEngine.ini` переключён `GlobalDefaultGameMode`
  с `BP_ThirdPersonGameMode` на **`/Script/Sandbox.ModularCarGameMode`**.
  (Карта `Lvl_PodRacer` гейммод в World Settings НЕ переопределяет — проверено grep'ом.)
- В пешку добавлено: прямой опрос клавиатуры `PollKeyboard()` (W/S/A/D + Space) — проект на
  Enhanced Input без legacy-осей, так машина едет сразу; `SpawnCollisionHandlingMethod =
  AdjustIfPossibleButAlwaysSpawn` (без него спавн отменялся: «failed because of collision at 0,0,0»);
  `AutoPossessPlayer = Player0`.
- Проверено в редакторе: пешка **спавнится и поссешится** (`APlayerController::OnPossess` для ModularCarPawn).

### Известные проблемы / на чём остановились
- PIE проекта настроен на **3 сетевых клиента** → при динамическом спавне кластер-пешки сыплет
  Iris-репликацией: `Actor ModularCarPawn ... is not initialized and won't be replicated` +
  `Ensure: ChildRefHandle.IsValid()` в `OnPossess`. Не фатально, но для чистого теста
  ставить PIE = **1 игрок / Standalone** (Editor → дропдаун Play → Number of Players = 1, Net Mode = Standalone).
- Для нетплея позже: разобраться с репликацией ClusterUnion-пешки под Iris
  (либо корректно инициализировать репликацию дочерних прокси, либо использовать схему
  async-networked physics как в `MyPhysicsPawn` через `UNetworkPhysicsComponent`).
- `Ensure: EditorHash` (WorldPartition) — движковый шум PodRacer, не наш код.
- Запуск: `UnrealEditor.exe "Z:\NetworkedPhysics\Sandbox.uproject" -noxgeshadercompile`.

---

## Текущая архитектура ModularCarPawn + фикс катапульты подвески (2026-06-18)

**Архитектура (актуальная, заменила ClusterUnion):** одиночное симулируемое твёрдое тело.
- Корень пешки — `UStaticMeshComponent Body` (примитив Cube, `SetSimulatePhysics(true)`,
  `QueryAndPhysics`, `ECC_PhysicsBody`, блокирует всё). Масса по умолчанию 1200 кг.
- Колёса — `UStaticMeshComponent` (Cylinder), **`NoCollision`** — только визуал + точка опоры
  для луча. Add/remove в рантайме через `AddWheel/RemoveWheel/RemoveLastWheel` (BlueprintCallable).
- Подвеска — **raycast** вниз от каждого колеса (`LineTraceSingleByChannel`, `ECC_WorldStatic`),
  длина луча `SuspensionRestLength + Radius` = 90 ед. Силы (пружина+демпфер push-only, тяга,
  боковой/продольный захват, руль) прикладываются к `Body` через `AddForceAtLocation` в точке колеса.
- Камера: `SpringArm` (900, лаг) + `Camera`. Ввод: Enhanced Input (`PawnClientRestart` →
  `EnsureInputAssets` создаёт IMC/IA в коде на W/S/A/D/Space) + дублирующий `PollKeyboard()` опрос.

**Найденная и устранённая проблема — катапульта подвески.**
- Симптом (headless self-test, см. ниже): машина спавнится на Z=300, падает, затем **выстреливает**
  вверх до Z≈486 и дальше скачет в предельном цикле (185–254), периодически теряя контакт
  (`Grounded=0/4`). XY-движение есть (тяга работает), но машина «прыгает», а не едет.
- Причина: пружинная сила была зажата на `WeightPerWheel * 4` (≈4× полного веса), а демпфер при
  движении вниз ещё добавлял → суммарно ~3g вверх. Машину выбрасывало **выше длины луча (90 ед.)**,
  после чего колёса не достают до земли → баллистическое падение → снова катапульта.
- **Фикс:** в `UpdateVehiclePhysics` зажим пружины снижен до `WeightPerWheel * 2.0f`
  (`ModularCarPawn.cpp`, блок «1) Suspension spring»). Демпфирование 3000 ≈ критическое для
  m≈300 кг/колесо, k=9000 (ζ≈0.9) — менять не понадобилось.

**Self-test (регрессия, headless).** CVar `car.SelfTest 1`:
- После 1 с осадки даёт **газ 0.6 + руль 0.6** (езда по кругу — машина остаётся на конечной
  тестовой платформе и заодно проверяется руль; прямой полный газ просто уезжал за край).
- Логирует траекторию и в конце `SELFTEST RIDE | MinZ MaxZ Band` — диапазон высоты за фазу езды.
- Запуск:
  ```
  UnrealEditor.exe "Z:\NetworkedPhysics\Sandbox.uproject" /Game/Lvl_ModularCarTest -game ^
    -ExecCmds="car.SelfTest 1" -unattended -nopause -nosplash -noxgeshadercompile -log
  ```
- **Результат после фикса:** `MinZ=98 MaxZ=203 Band=105`, MovedZ≈−174 (это только осадка со
  спавна 300→126), машина едет по дуге и **не падает с платформы** (раньше улетала на Z=−4163).
  Катапульты нет. До фикса Band был бы ~380+ и провал в пустоту.

### На чём остановились / следующие шаги
- Подвеска стабильна, но Band≈105 — машина ещё слегка раскачивается (до ~+100 над равновесием
  ~Z=102). При желании ужать: чуть поднять демпфер (3000→~3500) или снизить жёсткость.
- Проверить в **редакторе вживую** (PIE = 1 игрок / Standalone): possess, W/S/A/D/Space, и
  рантайм `AddWheel`/`RemoveWheel`.
- Сетевая репликация пешки под Iris — всё ещё открытый вопрос (см. секцию выше).
- Сборка: `Build.bat SandboxEditor Win64 Development -Project=...Sandbox.uproject -WaitMutex` (~5–13 с).

---

## Машина перестала дёргаться: пружинную подвеску заменили на «ящик на земле» + автотесты (2026-06-18, поздно)

> ⚠️ Секции выше про raycast-подвеску (пружина+демпфер на колёса) — **ИСТОРИЯ**. Та модель так и
> не стала спокойной в покое; ниже описано рабочее решение. Поля `Suspension*` и `FrictionCoeff`
> из кода удалены.

**Жалоба пользователя:** «машина всё время дёргается, в покое должна стоять спокойно». Сделать
автотесты и добиться спокойствия.

**Диагностика (через тест покоя `car.SelfTest 2`).** Даже без газа машина блуждала по платформе
(скорость до 600 см/с) и подпрыгивала (ZBand 50–65). Разобрали по слоям:
1. Силы захвата делились на `DeltaTime` и прикладывались в смещённой точке → на 200 FPS это
   гигантская сила в точке → крутящий момент → вращение → скорость в точке → ещё больше силы
   (само-раскрутка). → перешли на частото-независимое демпфирование `F=-v·m·rate`.
2. Пружину прикладывали вдоль оси кузова `Up` → при наклоне появлялась горизонтальная составляющая
   → дрейф. → перенесли на нормаль контакта `Hit.ImpactNormal`.
3. Зажим суммарной пружинной силы на 2× срезал диссипативный демпфер → каждый «прижим» отыгрывал
   упруго → вечный бобинг. Снять зажим → демпфер в дискретном времени **реверсирует** скорость →
   катапульта (Z взлетал до 7000). Любой компромисс по зажиму давал либо бобинг, либо катапульту.
4. Силы захвата перенесли в центр масс (убрали петлю крена) — горизонтальный дрейф ушёл, но
   **вертикально-креновая** неустойчивость пружины осталась: при возмущении колесо отрывается →
   пропадает его пружина → асимметрия → выброс. Нелинейный предельный цикл, тюнингом не лечится.

**Вывод и решение.** Самодельная raycast-пружина имеет стойкую многомодовую неустойчивость.
Заменили её на **модель «ящик на земле»**: кузов (одно тело `Body`) опирается на землю через
штатный контактный солвер Chaos (покоящееся тело абсолютно стабильно — это движок делает идеально).
Никакой пружины. Силы только горизонтальные, **все в центре масс** (`AddForce`, не AddForceAtLocation):
- тяга `Fwd·throttle·EngineForcePerWheel·NumWheels`;
- боковой захват — демпфирование боковой скорости (`-Right·v·m·LateralGripRate`);
- продольное торможение/откат при холостом ходе/ручнике (`LongitudinalGripRate`);
- руль — **отдельный момент рыскания** вокруг `Up`, масштабируется скоростью (`AddTorqueInRadians`,
  bAccelChange) → поворачивает только на ходу и естественно в реверсе.
Доп.: `SettleOntoGround()` ставит кузов сразу на землю (без удара со спавна); кузову назначен
низкофрикционный физматериал (`Friction 0.2`), чтобы движением управляли наши силы; мёртвая зона
`RestSpeedDeadzone` → при малой скорости силы выключаются и ящик засыпает = полный покой.
Колёса-цилиндры остались чисто косметикой (`NoCollision`).

**Автотесты (headless, CVar `car.SelfTest`).**
- `car.SelfTest 1` — DRIVE: после осадки газ+руль, печатает `SELFTEST RIDE | MinZ MaxZ Band`.
- `car.SelfTest 2` — REST: руки прочь от управления, 4 с осадки + 5 с замера, печатает
  `SELFTEST REST | PeakSpeed ZBand => PASS/FAIL` (пороги: скорость ≤5 см/с, ZBand ≤3 см).
- Запуск любого:
  ```
  UnrealEditor.exe "Z:\NetworkedPhysics\Sandbox.uproject" /Game/Lvl_ModularCarTest -game ^
    -ExecCmds="car.SelfTest 2" -unattended -nopause -nosplash -noxgeshadercompile -log
  ```
  Парсить строки `SELFTEST` из `Saved\Logs\Sandbox.log`.

**Доработка «машина лежала на брюхе» → теперь стоит на колёсах.** Первая версия box-on-ground
опиралась плоским днищем куба — визуально машина лежала пузом, колёса проваливались в землю.
Исправление: у колёс-цилиндров **включена коллизия** и они **приварены** к кузову
(`AttachToComponent(..., FAttachmentTransformRules(KeepRelative, /*bWeld*/true))`) — это по-прежнему
ОДНО жёсткое тело, но опирается на 4 колеса (как стол на ножках; солвер держит стабильно), кузов
приподнят. Сопутствующее:
- Колёсам назначен тот же низкофрикционный физматериал. ВАЖНО: трение между поверхностями
  комбинируется (по умолчанию Average), поэтому 0.2(колесо) усреднялось с ~0.7(земля) до ~0.45 →
  тяга 600k не могла стронуть машину. Поставили `bOverrideFrictionCombineMode=true;
  FrictionCombineMode=Min` → доминирует наше низкое трение → машина едет.
- `RestRideHeight` = насколько нижняя точка колёс ниже центра кузова; по нему `SettleOntoGround`
  ставит машину на колёса, и по нему же луч проверки «на земле» (`RestRideHeight + 90`) достаёт до
  земли (иначе тяга считала машину «в воздухе» и не прикладывалась).
- Сварка колёс подняла суммарную массу (~1634 кг вместо 1200 — `SetMassOverrideInKg` после сварки
  не удержал; не критично, влияет только на разгон).

**Результаты (оба зелёные, машина на колёсах):**
- REST: `PeakSpeed=0.0 ZBand=0.0 => PASS` — стоит как вкопанная на колёсах (Z≈159, позиция не меняется).
- DRIVE: `MovedXY≈2542, Band=0` — едет (разгон до ~660 см/с), поворачивает дугой, высота неподвижна.
  Катапульты/бобинга/дёрганья нет.

### Настройка (Details, категории Modular Car|*)
- Body: `BodyExtent`, `BodyMassKg`, `ChassisAngularDamping=3`, `ChassisLinearDamping=0.2`.
- Drive: `EngineForcePerWheel=250000` (итого ×4 колеса), `MaxYawAccel=120` (deg/s²),
  `SteerRefSpeed=350` (см/с — скорость полной рулёжки), `LateralGripRate=12`, `LongitudinalGripRate=4`,
  `RestSpeedDeadzone=3`.

### Следующие шаги
- При желании сделать разгон живее — поднять `EngineForcePerWheel` или починить `SetMassOverrideInKg`
  после сварки (сейчас масса ~1634).
- Проверить вживую в редакторе (PIE = 1 игрок / Standalone) + рантайм `AddWheel`/`RemoveWheel`.
- Iris-репликация пешки — по-прежнему открыта.

---

## Мультиплеер: ModularCarPawn переведён на Chaos networked physics (2026-06-18, ещё позже)

**Запрос пользователя:** «нет мультиплеерной поддержки, нам нужен был мультиплеер». Верно — предыдущая
версия была чисто одиночной (все силы в игровом `Tick`, без репликации).

**Что сделано.** Переписал `ModularCarPawn` по схеме эталонного `AMyPhysicsPawn` (Chaos networked
physics с клиентским предсказанием и серверной ресимуляцией):
- Вся физика машины (тяга, боковой/продольный захват, руль) перенесена из игрового `Tick` в
  **async sim-callback физпотока** `FModularCarAsync::OnPreSimulate_Internal`, силы прикладываются к
  `Chaos::FPBDRigidParticleHandle` (`AddForce`/`AddTorque`). Логика та же, что в одиночной box-on-ground
  версии: горизонтальные силы в центр масс, руль — момент рыскания (масштаб по скорости).
- Ввод (`Throttle/Steer/Handbrake`) гоняется через `UNetworkPhysicsComponent` + структуры
  `FNetInputModularCar : FNetworkPhysicsPayload` (Interpolate/Merge/Decay/Compare) и пустой
  `FNetStateModularCar`. `FAsyncInputModularCar/Output` — game↔physics маршалинг.
- `PostInitializeComponents`: создаёт `FModularCarAsync` через
  `Solver->CreateAndRegisterSimCallbackObject_External`, кладёт `PhysicsObject` (корневой Body),
  копирует тюнинг (сила/захват/руль/инерция рыскания) в async-объект, регистрирует
  `CreateDataHistory<FNetInputModularCar, FNetStateModularCar>`. `EndPlay` — unregister.
- `Tick` теперь только кормит `GetProducerInputData_External()` локальным вводом
  (`if IsLocallyControlled`), физику не трогает. Частица — `NeverSleep` (нужно для ресима).
- Конструктор: `bReplicates=true`, `NetworkPhysicsComponent` создаётся только при
  `bEnablePhysicsPrediction` (включено — CARSETUP лог `Net=1`). Убраны `SetReplicatingMovement`
  и `AutoPossessPlayer` (состояние синхронит физкомпонент, поссешит гейммод per-player).

**Проверка.** Оба headless self-test'а (через async-путь, Net=1) зелёные:
- REST: `PeakSpeed=0.0 ZBand=0.0 => PASS` (спокойствие сохранено).
- DRIVE: `MovedXY≈2719, Band=0` — едет и поворачивает.
> Полную сеть (предсказание/откат сервер+клиент) headless-тесты НЕ покрывают — проверять
> **PIE с Number of Players = 2** (теперь должно работать корректно, в отличие от старого
> ClusterUnion-варианта).

### Следующие шаги
- Проверить PIE на 2 игрока: оба клиента видят и управляют своими машинами, плавность при пинге
  (Net Emulation → добавить задержку).
- Тюнинг разгона/руля (`EngineForcePerWheel`, `MaxYawAccel`) при необходимости.

---

## Устранение сетевого дёрганья: изучение кода движка + 2 правки (2026-06-18, ещё позже)

**Запрос:** «устрани подёргивания на сервере, изучи код движка как там всё реализовано и сделай так же».

**Что изучил в движке (Z:\UnrealEngineSource_5_7):**
- `NetworkPhysicsComponent.cpp`: порядок шага — `OnPreProcessInputs_Internal` (вызывает `ApplyInput`
  из истории: серверный ввод клиента / исторический ввод с затуханием при ресиме) → наш
  `ProcessInputs_Internal` → `OnPostProcessInputs_Internal` (`BuildInput` пишет в историю). Всё это
  ДО `OnPreSimulate`. Вывод: `*_Internal` уже корректно выставлен к моменту приложения сил.
- `CharacterMovementComponentAsync` (продакшн-эталон) читает consumer-ввод в `OnPreSimulate`.
- `PhysicsReplication.cpp`/`EPhysicsReplicationMode`: по умолчанию у актёра режим **`Default`** —
  реплицированная поза **защёлкивается** на тело каждым апдейтом (это и есть дёрганье). Для
  предсказания нужен **`Resimulation`** (откат+переигрыш от скорректированного состояния = плавно).

**Правка 1 — режим репликации физики (главная):** в конструкторе при включённом предсказании
`SetPhysicsReplicationMode(EPhysicsReplicationMode::Resimulation)`. Без этого тело оставалось на
`Default` → снэп на реплику → дёрганье. (`AMyPhysicsPawn` это не ставит — вероятно, поэтому и он
дёргается.)

**Правка 2 — детерминизм ввода при ресиме:** `OnPreSimulate_Internal` читает «живой»
`GetConsumerInput_Internal()` **только вне ресимуляции**; во время ресима использует `*_Internal`,
выставленный фреймворком из истории (`ApplyInput`). Чтение живого ввода в ресиме рассинхронит
переигрыш с оригинальным шагом → коррекции → дёрганье. `ProcessInputs_Internal` захватывает
локальный ввод в `*_Internal` для записи в историю (`BuildInput`).

**Диагностический факт:** headless self-test идёт в `-game` (standalone) — там `ProcessInputs_Internal`
НЕ вызывается и ресимов НЕТ (проверено логом). Значит self-test проверяет только силовую модель и
`OnPreSimulate`, но НЕ сетевой путь (история/ресим). Регресс одиночной физики проверен (REST PASS,
DRIVE едет), но **отсутствие сетевого дёрганья надо подтвердить вживую в PIE на 2 игрока** — headless
этого не покрывает.

### Следующие шаги
- PIE 2 игрока + Net Emulation (задержка/потери): убедиться, что дёрганья нет ни у своей машины
  (autonomous proxy, resim), ни у чужой (sim proxy, по умолчанию PredictiveInterpolation).
- Если у чужих машин (sim proxy) останется рывками — настроить `SimProxyRepMode` через
  `NetworkPhysicsSettingsComponent` (по умолчанию уже `PredictiveInterpolation`).

---

## ИТОГ СЕССИИ 2026-06-18 (на чём закончили)

Работали над `ModularCarPawn` (модульная физическая машина в проекте Sandbox/NetworkedPhysics).
Прошли путь: одиночная raycast-подвеска → стабильная модель «ящик на колёсах» → полноценный
мультиплеер на Chaos networked physics. Что сделано за сессию (по порядку):

1. **Фикс катапульты подвески** — снизили зажим пружины; машина перестала улетать в небо.
2. **Автотесты (headless, CVar `car.SelfTest`)**: `1` = заезд по кругу (`SELFTEST RIDE`),
   `2` = тест покоя с вердиктом `PASS/FAIL` (`SELFTEST REST`). Запуск:
   `UnrealEditor.exe ...Sandbox.uproject /Game/Lvl_ModularCarTest -game -ExecCmds="car.SelfTest 2"
   -unattended -nopause -nosplash -noxgeshadercompile -log`, парсить строки `SELFTEST` в
   `Saved\Logs\Sandbox.log`.
3. **Машина перестала дёргаться в покое** — после длинной диагностики автоколебаний самодельной
   пружины перешли на модель **«ящик на земле»** (кузов опирается через контактный солвер движка).
   Тест покоя: `PeakSpeed=0.0 ZBand=0.0 => PASS`.
4. **Машина встала на колёса** (не на брюхо) — колёсам включили коллизию и приварили к кузову
   (одно тело на 4 опорах); низкое трение + `FrictionCombineMode=Min`, чтобы ехала.
5. **Мультиплеер** — переписали на Chaos networked physics (async-колбэк физпотока +
   `UNetworkPhysicsComponent` + структуры предсказания), как эталонный `AMyPhysicsPawn`.
6. **Устранение сетевого дёрганья** — изучили код движка; ключевое:
   `SetPhysicsReplicationMode(EPhysicsReplicationMode::Resimulation)` (вместо `Default`-снэпа) +
   детерминизм ввода при ресиме.

**Состояние кода:** компилируется (`Build.bat SandboxEditor ...` → Succeeded ~10 с). Одиночные
автотесты зелёные (REST PASS, DRIVE едет/поворачивает, ride height стабилен).

**ГЛАВНОЕ НЕЗАКРЫТОЕ:** сетевое поведение (плавность при пинге, отсутствие дёрганья на сервере/
клиенте) **НЕ проверено** — headless self-test идёт в standalone и сетевой путь (история ввода/
ресим) не задействует. Следующая сессия: **запустить PIE с Number of Players = 2** (+ Net Emulation)
и убедиться, что дёрганья нет; при необходимости донастроить `SimProxyRepMode`.

**Файлы:** `Source/Sandbox/ModularCarPawn.{h,cpp}`, гейммод `ModularCarGameMode`, карта
`Content/Lvl_ModularCarTest.umap`. Запуск редактора всегда с `-noxgeshadercompile`.

---

## Нормальные меши: кузов SportsCar + колёса поверх физ-прокси (2026-06-20)

**Запрос пользователя:** заменить примитивы (куб + цилиндры) на нормальную кабину и колёса из
дефолтных пакетов Анрила.

**Подход — визуальный оверлей (физику НЕ трогаем).** Тонко настроенная модель «ящик на колёсах»
осталась как есть: куб `Body` и цилиндры-колёса по-прежнему симулируют/коллайдят, но становятся
**невидимыми**, а поверх накладываются косметические меши из проектного Chaos-Vehicle контента:
- Кузов: `BodyVisual` (UStaticMeshComponent) с `/Game/ThirdPerson/Blueprints/StaticMeshes/SportsCar_SM`,
  `NoCollision`, **absolute scale** (чтобы неравномерный масштаб кузова из `BodyExtent` не плющил арт).
- Колёса: на каждое колесо в `AddWheel` создаётся `FCarWheel.VisualMesh` = `SportsCarWheel_SM`
  (`CreateWheelVisual`), `NoCollision`, absolute scale, та же `RelativeLocation`, что и у цилиндра-опоры,
  правые колёса зеркалятся по Yaw +180°. `RemoveWheel` теперь сносит и визуал.
- Куб прячется `Body->SetVisibility(false)` в `BeginPlay`; цилиндры — `SetVisibility(false)` в
  `CreateWheelMesh`. Если меш не найден (`FObjectFinder` не Succeeded) — тихий откат на голые прокси.

**Тюнинг (Details → Modular Car|Visuals):** `bUseVisualMeshes`, `BodyVisualScale/Offset/Rotation`,
`WheelVisualScale`, `WheelVisualRotation`. Точную подгонку (масштаб/смещение/поворот арта к прокси)
надо добивать вживую в редакторе — размеры/пивоты мешей из `.uasset` оффлайн не прочитать; дефолты
заданы разумные (scale 1, offset 0).

**Проверка.** Сборка `SandboxEditor` — `Result: Succeeded` (~29 с, ModularCarPawn.cpp чисто,
единственный warning — предсуществующий в `VehicleSimRotatorComponent.cpp`). Headless REST-самотест
(`car.SelfTest 2`) — `PeakSpeed=0.0 ZBand=0.0 => PASS`, `Mass=1634 Wheels=4 Net=1`, без ошибок.
То есть оверлей не повлиял на физику.

### Следующие шаги
- Открыть `Lvl_ModularCarTest` в редакторе (PIE 1 игрок/Standalone) и подогнать `Modular Car|Visuals`:
  скорее всего нужен `BodyVisualOffset.Z` (поднять/опустить кузов на прокси) и проверить ориентацию
  колёс (`WheelVisualRotation`/зеркалирование сторон).
- Косметика: визуальные колёса пока НЕ вращаются (модель и так не катит колёса — едет на силах
  захвата). При желании — крутить визуал по скорости/рулю в Tick.
- Сетевой тест на 2 игрока (PIE) — по-прежнему открыт (см. секции выше).

---

## КОРРЕКЦИЯ мешей: SportsCar_SM битый → перешли на скелетный SKM_SportsCar + авто-тест визуала (2026-06-20, позже)

> ⚠️ Секция выше («Нормальные меши: кузов SportsCar...») описывала использование **статических**
> `SportsCar_SM`/`SportsCarWheel_SM` из `/Game`. Они НЕ РАБОТАЮТ — см. ниже.

**Симптом:** пользователь сообщил «нет изменений, всё ещё куб и цилиндры». Физ-тесты (1/2) этого не
ловят (проверяют только физику).

**Диагностика — добавлен авто-тест визуала `car.SelfTest 3`** (запускает движок, проверяет, что
косметика реально применилась: кузов-арт на месте, куб скрыт, цилиндры скрыты; печатает
`SELFTEST VISUAL ... => PASS/FAIL`). Первый прогон: `CarBodyMesh=NULL ... => FAIL`. По логу загрузчика:
```
LogLinker: Error: SportsCar_SM.uasset: Package was saved with a newer custom version than the current.
  Name 'FortniteMain' PackageVersion 253  MaxExpected 225
LogAssetRegistry: Error: Package is unloadable: ... Custom version is too new
```
**Корень:** `SportsCar_SM`/`SportsCarWheel_SM` (и Pod/Truck-пешки, GC_SportsCarWheel*) сохранены
**более новой версией движка** (FortniteMain v253 > 225 у этого UE 5.7.4) и в принципе не грузятся
в этой сборке. Кодом не лечится. (FObjectFinder в конструкторе тоже молча давал NULL — отдельно
переведено на рантайм-загрузку, но и `LoadObject` падал по той же причине.)

**Решение:** взяли нативный 5.7 меш из плагина движка — **`/ChaosModularVehicleExamples/Models/SportsCar/SKM_SportsCar`**
(плагин `ChaosModularVehicleExamples` включён). Это **скелетный** меш всей машины (кузов + колёса).
- `BodyVisual` теперь `USkeletalMeshComponent` (ref-поза, `AnimationSingleNode`, `NoCollision`,
  absolute scale), грузится в `BeginPlay` через `LoadObject<USkeletalMesh>`.
- Куб и все цилиндры скрываются (`SetVisibility(false)`) когда `SKM` загружен; физика/коллизия не
  тронуты. Отдельные визуальные колёса убраны (SKM уже содержит колёса) — поля
  `WheelVisual*`/`CreateWheelVisual`/`FCarWheel.VisualMesh` удалены.
- Тюнинг арта: `Modular Car|Visuals` → `bUseVisualMeshes`, `BodyVisualScale/Offset/Rotation`.

**Проверка (оба зелёные):**
- `car.SelfTest 3` (визуал): `CarMesh=SKM_SportsCar BodyArtApplied=1 hiddenCube=1 hiddenCylinders=4/4 => PASS`.
- `car.SelfTest 2` (REST): `PeakSpeed=0.0 ZBand=0.0 => PASS` — физика не пострадала.

### Следующие шаги
- В редакторе подогнать `Modular Car|Visuals` (масштаб/смещение/поворот) — арт SKM может не совпадать
  по размеру/позе с боксом-прокси; скорее всего нужен `BodyVisualScale` и `BodyVisualRotation.Yaw`.
- Битые ассеты (`SportsCar_SM`, Pod/Truck-пешки, `02_-_Default`) шумят в логе «Package is unloadable» —
  их стоит удалить из проекта или пересохранить в движке, которым они создавались.
- Косметика: SKM рендерится в ref-позе, колёса не вращаются/не рулят визуально (физика — на силах).

---

## Руление: слабый поворот → модель «целевая скорость рыскания» + замер в DRIVE-тесте (2026-06-20, ещё позже)

**Запрос:** «угол поворота передних колёс очень маленький» — машина поворачивает слабо.

**Диагностика (добавлен замер в DRIVE-тест `car.SelfTest 1`):** логируем `PeakYawRate` (deg/s). При
старой торк-модели даже после ×3 по `MaxYawAccel` (120→360) пик был всего **5.7 deg/s** — момент
`Steer·SpeedFrac·MaxYawAccelRad·YawInertia` через `AddTorque` почти не доходил: реальная инерция
сваренного тела (куб+4 цилиндра, 1634 кг) много больше оценки (считали от 1200 кг), плюс угловое
демпфирование. Калибровать торк вслепую — гадание.

**Решение — аркадная модель руления по целевой угловой скорости.** В `FModularCarAsync::OnPreSimulate_Internal`
вместо `AddTorque` теперь задаём целевую скорость рыскания: `W.Z = Lerp(W.Z, Steer·SpeedFrac·MaxYawRateRad, 0.5)`
(pitch/roll не трогаем — их держит солвер). Детерминированно (ок для предсказания/ресима),
включается только при вводе руля и масштабируется скоростью (`SteerRefSpeed`), в реверсе
поворачивает естественно.
- Параметр `Modular Car|Drive`: `MaxYawAccel` (deg/s²) заменён на **`MaxYawRate` (deg/s, по умолчанию 80)** —
  это и есть «насколько круто поворачивает». `SteerRefSpeed` 350→**250** (полный поворот раньше).
- В async убраны `MaxYawAccelRad`/`YawInertia`, добавлен `MaxYawRateRad`.

**Проверка:**
- DRIVE `car.SelfTest 1`: `PeakYawRate=45.4 deg/s` при рулёжке 0.6 (цель 0.6·80=48 — совпадает),
  было 5.7 → теперь поворот в ~8 раз круче; высота стабильна (`Band=0`).
- REST `car.SelfTest 2`: `PeakSpeed=0.0 ZBand=0.0 => PASS` — покой не сломан.

### Следующие шаги
- `MaxYawRate=80` — стартовое; крутить под вкус (тайтовее — 100–120, спокойнее — 50–60). На полном
  руле (1.0) пик ≈ MaxYawRate.
- Визуально передние колёса SKM не поворачиваются (это арт в ref-позе; поворачивается весь кузов).
  Если нужно именно вращать колёса-кости — отдельная задача (нужны имена костей колёс SKM_SportsCar).

---

## Дёрганье после добавления кабины: косметический SKM создавал физ-тела (2026-06-20, ещё позже)

**Жалоба:** после добавления кабины (скелетный `SKM_SportsCar`) машину стало дёргать.

**Диагностика (расширен REST-тест `car.SelfTest 2`):** добавлен замер **вращательного** дрожания —
`PeakAngSpeed` (deg/s угловой скорости) и `MaxTilt` (наклон кузова от вертикали), плюс ужесточены
пороги PASS (AngSpeed ≤5, Tilt ≤2). Прогон с кабиной: `PeakSpeed=0 ZBand=0 PeakAngSpeed=0 MaxTilt=0`
— в headless-standalone дёрганья НЕТ вообще. Значит сама физика шасси стабильна, а тряска — на
стороне рендера/сети.

**Корень:** `SKM_SportsCar` тянет `SportsCar_PhysicsAsset`. `USkeletalMeshComponent` по умолчанию
инстанцирует кинематические физ-тела этого ассета. Они обновляются на частоте игрового кадра, тогда
как шасси шагает на async-физпотоке с суб-степпингом → тела кабины «отстают» от шасси → визуальная
тряска. (Массу шасси при этом не меняли — `Mass=1634` до и после, т.е. не приваривались.)

**Фикс — кабина строго косметическая:** в конструкторе `BodyVisual->SetSimulatePhysics(false)` +
`SetEnableGravity(false)`; в `ApplyBodyVisual()` после `SetSkeletalMeshAsset` →
`SetSimulatePhysics(false)`, **`SetPhysicsAsset(nullptr)`**, `SetCollisionEnabled(NoCollision)`.
Никаких физ-тел у кабины → она просто жёстко следует за шасси.

**Проверка:** mode 3 (визуал) `=> PASS` (кабина применена, прокси скрыты), mode 2 (REST)
`PeakSpeed=0 ZBand=0 PeakAngSpeed=0 MaxTilt=0 => PASS`. Регрессий нет.

> ВАЖНО: headless `-game` рендер-тряску не воспроизводит — фикс надо подтвердить вживую в PIE.
> Если дёрганье останется ИМЕННО в мультиплеерном PIE (2+ игрока) — это уже отдельный открытый
> вопрос сетевого ресима (см. секции про networked physics), не кабина.

---

## Столкновение машин: «прилипание»/слом физики → friction-circle на захват + авто-тест (2026-06-20, ещё позже)

**Жалоба:** при сближении двух машин физика ломается, чужая машина «прилипает».

**Корень:** силы захвата применялись как `-velocity·mass·rate` **без предела** — бесконечный «якорь».
Когда машина A таранит B, у B эта сила сопротивляется толчку любой величиной → B не сдвигается
(«прилипает»), A продолжает давить газом → гигантские контактные силы → солвер разносит физику.

**Фикс — ограничение захвата (friction circle):** боковая и продольная силы захвата зажаты на
`MaxGripAccel·mass` (новый параметр `Modular Car|Drive → MaxGripAccel`, по умолчанию 1500 см/с² ≈ 1.5g).
Теперь захват ведёт себя как реальная шина: держит до предела, а достаточно сильный толчок
пересиливает его и машину просто сдвигает, без слома физики. (`OnPreSimulate_Internal`, блок сил.)

**Авто-тест `car.SelfTest 4` (COLLISION):** ведущая (player) машина спавнит вторую (пассивную)
в 700 см впереди и таранит её на полном газу. Меряет: пиковую скорость любой из машин (взрыв),
макс. Z (подброс), мин. дистанцию между центрами (проваливание сквозь), сдвиг цели (толкнулась ли).
Спавненная машина не оркестрирует тест (гейт `if (!IsLocallyControlled()) return;`).
Пороги PASS: PeakSpeed ≤2500, MaxZ ≤spawn+300, MinSep ≥150, TargetPushed ≥50.

**Проверка (все зелёные):**
- COLLISION: `PeakSpeed=425 MaxZ=159 MinSep=440 TargetPushed=290 => PASS` — контакт в t≈2.7 (Sep≈440=
  длина машины), дальше цель плавно толкается (X 700→976, Sep стабильно 440), без взрыва/подброса/туннеля.
- REST: `PeakSpeed=0 ZBand=0 PeakAngSpeed=0 MaxTilt=0 => PASS`.
- DRIVE: `Band=0 PeakYawRate=45.4 deg/s` (руление не задето).

### Сводка self-test'ов (CVar `car.SelfTest`)
1 = заезд по кругу (RIDE + PeakYawRate), 2 = покой (REST, линейный+угловой), 3 = косметический меш (VISUAL),
4 = столкновение (COLLISION). Запуск: `UnrealEditor.exe ...Sandbox.uproject /Game/Lvl_ModularCarTest -game
-ExecCmds="car.SelfTest N" -unattended -nopause -nosplash -noxgeshadercompile -log`, парсить `SELFTEST` в Sandbox.log.

### Следующие шаги
- `MaxGripAccel=1500` — стартовое; ниже = легче толкать/дрифтить, выше = цепче (но при слишком высоком
  вернётся «прилипание»). Тюнить под вкус.
- Подтвердить вживую в мультиплеерном PIE (2 игрока) — столкновения предсказанных тел по сети это
  headless-тест не покрывает.

---

## Сетевой тест-харнесс для залипания машин (2026-06-20, ещё позже)

**Жалоба:** в мультиплеере машины слипаются и не разъезжаются. Standalone-тесты (4/5) залипание НЕ
ловят, т.к. `-game` не задействует предсказание/ресим/sim-proxy.

**Сначала — фикс силовой модели (см. секцию про friction-circle):** силы захвата зажаты на
`MaxGripAccel·mass`, иначе безлимитный «якорь» при таране ломал контакт. Standalone-тесты PASS.

**Сетевой харнесс (headless, 2 процесса, `-nullrhi`):**
- `car.SelfTest 6` (NETCOLLISION): на **сервере** (dedicated) спавнится стационарная машина-цель
  перед машиной игрока; на **клиенте** цель — sim-proxy; клиентская (предсказанная) машина таранит
  её и отъезжает. Замер на клиенте: пик скорости, мин. дистанция, **джиттер sim-proxy**, расхождение
  при отъезде. Серверная/целевая машины тест не оркестрируют (гейты `bIsNetTarget` / `GetController()`).
- Запуск (PowerShell): dedicated server `... /Game/Lvl_ModularCarTest -server -nullrhi
  -ExecCmds="Net PktLag=90, car.SelfTest 6" -LOG=NetS.log -port=7777`, затем клиент
  `... 127.0.0.1:7777 -game -nullrhi -ExecCmds="Net PktLag=90, car.SelfTest 6" -LOG=NetC.log`.
  Парсить `SELFTEST NETCOLLISION` в клиентском логе. (Удаление логов через pipeline блокируется
  песочницей — пишем логи с временной меткой, не удаляем.)

**Результаты — залипание НЕ воспроизвелось:**
- Localhost (пинг ~0): `TargetJitter=0.7 MinSep=440 SepGainOnRetreat=572 => PASS`.
- С лагом 90 мс: `TargetJitter=5.2 MinSep=440 SepGainOnRetreat=572 => PASS`. Цель толкается и чисто
  отлипает при отъезде. Связь поднимается (Join succeeded), цель реплицируется.

**Вывод:** сценарий «предсказанное тело vs реплицируемая машина» стабилен — friction-circle, похоже,
убрал причину. НЕ покрыт случай **взаимного предсказания двух клиентских машин** (dedicated + 2
клиента, обе предсказываются и сталкиваются) — для него нужен 3-процессный харнесс (mode 7, TODO).

### Следующие шаги
- Пересобрать (`Build.bat SandboxEditor`) и **перезапустить редактор**, проверить PIE на 2 игрока
  заново — возможно, баг уже ушёл с friction-circle фиксом.
- Если залипание осталось ИМЕННО при двух игроках-клиентах — собрать 3-процессный тест (mode 7,
  head-on двух предсказанных машин) и тогда чинить sim-proxy репликацию (NetworkPhysicsSettingsDataAsset:
  SimProxyRepMode / bApplySimProxyStateAtRuntime).

---

## Откат кабины к кубу (2026-06-20, ещё позже)

**Запрос:** залипание осталось → откатить, вернуть кабины машин в виде куба как было раньше.

**Сделано:** `bUseVisualMeshes` по умолчанию = **false**. Скелетная `SKM_SportsCar` не грузится и не
показывается; куб-кузов и цилиндры-колёса снова видимы (прежний вид). Код косметической машины
оставлен под флагом — можно вернуть, поставив `bUseVisualMeshes=true`. `car.SelfTest 3` сделан
адаптивным (проверяет либо «куб виден / нет арта», либо «арт применён / прокси скрыты»).

**Проверка:** mode 3 `UseVis=0 cubeVisible=1 cylindersVisible=4/4 => PASS`; mode 2 REST
`PeakSpeed=0 ZBand=0 => PASS`.

> ВАЖНО: кабина — **косметика без коллизии**, на физику не влияла (масса шасси 1634 не менялась с
> ней и без). Поэтому откат к кубу НЕ устраняет мультиплеерное залипание — это отдельный сетевой
> вопрос (взаимное предсказание двух клиентских машин). Для него остаётся 3-процессный харнесс
> (mode 7) + настройка sim-proxy репликации.
